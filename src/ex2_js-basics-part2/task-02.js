var logArrElements = function(arr) {
    console.log("Amount of elements: " + arr.length);

    for (var i = 0; i < arr.length; i++) {
        console.log(arr[i]);
    }
}

module.exports = logArrElements;